function createUserArray(randomNumbers){	//declare user inputs	var user1 = parseInt($("#userNumber1").val());	var user2 = parseInt($("#userNumber2").val());	var user3 = parseInt($("#userNumber3").val());	var user4 = parseInt($("#userNumber4").val());	var userArray = [user1, user2, user3, user4];	var bullCount = 0;	var cowCount = 0;	//check for number of bulls	for (x=0; x <=3; x++) {		if (userArray[x] == randomNumbers[x]){		bullCount+=1;		}	};	//check for number of cows	for (x=0; x <= 3; x++) {		for (i=0; i <= 3; i++) {			if (x!=i && userArray[x] == randomNumbers[i]){				cowCount+=1;			}		}		};return [userArray, cowCount, bullCount];}function checkForWin(bullCount) {	if (bullCount == 4) {		alert("YOU WON! Click New Game to play again");	}	return false;}function createRandomNumbers() {	//declare numbers to guess	var firstNumber = Math.floor(Math.random() * 10);	var secondNumber = Math.floor(Math.random() * 10);	while (secondNumber == firstNumber) {		secondNumber = Math.floor(Math.random() * 10);	};	var thirdNumber = Math.floor(Math.random() * 10);	while (thirdNumber == firstNumber || thirdNumber == secondNumber) {		thirdNumber = Math.floor(Math.random() * 10);	};	var fourthNumber = Math.floor(Math.random() * 10);	while (fourthNumber == firstNumber || fourthNumber == secondNumber  || fourthNumber == thirdNumber) {		fourthNumber = Math.floor(Math.random() * 10);	};	var randomNumbers = [firstNumber, secondNumber, thirdNumber, fourthNumber];	return randomNumbers;}function writeNewTableRow(userArray, cowCount, bullCount, guessCount) {	$('#outputTable tr:last').after("<tr><td>" + guessCount + "</td><td>" + userArray + "</td><td>" + cowCount + "</td><td>" + bullCount + "</td></tr>");}function replaceTable() {	$('#outputTable').replaceWith("<table id='outputTable'><colgroup><col style='width: 5%;'><col style='width: 45%;'><col style='width: 25%;'><col style='width: 25%;'></colgroup><tbody><tr><th>Guess</th><th>Your Guess</th><th>Cows</th><th>Bulls</th></tr></tbody></table>");}function runValidation() {	// Object containing the validation rules	var userPlayRules = 		{		// The "name" attribute of the form control		userNumber1:					{ 			// Make this field required			required: true,			min: 0,			max: 9,			digits: true			},		userNumber2:					{ 			// Make this field required			required: true,			min: 0,			max: 9,			digits: true			},		userNumber3:					{ 			// Make this field required			required: true,			min: 0,			max: 9,			digits: true			},		userNumber4:					{ 			// Make this field required			required: true,			min: 0,			max: 9,			digits: true			}		};		// Object containing the error messages	var errorMessages = 	{		// The "name" attribute of the form control		userNumber1:		{			// The message to display			required: "You must enter a number",			min: "Number cannot be lower than 0",			max: "Number cannot be greater than 9",			digits: "Sorry, you can only enter numbers."		},		userNumber2:		{			// The message to display			required: "You must enter a number",			min: "Number cannot be lower than 0",			max: "Number cannot be greater than 9",			digits: "Sorry, you can only enter numbers."		},		userNumber3:		{			// The message to display			required: "You must enter a number",			min: "Number cannot be lower than 0",			max: "Number cannot be greater than 9",			digits: "Sorry, you can only enter numbers."		},		userNumber4:		{			// The message to display			required: "You must enter a number",			min: "Number cannot be lower than 0",			max: "Number cannot be greater than 9",			digits: "Sorry, you can only enter numbers."		}	};		// Object containing the entire validation configuration	var config = 	{		submitHandler: submitButton,		rules: userPlayRules,		messages: errorMessages	};		// Pass the config object to the form's validate() method	$("#theForm").validate(config);	return false;}